---
get:
  operationId: get-catalog-attribute-languages
  summary: "Get languages of a single attribute"
  description: "Rate limit: 1 request(s) in 5 second(s)"

  tags:
    - Catalog API

  responses: {
    $ref: "../../../responses/responses.yaml"
  }

parameters:
  # path parameters
  - $ref: "../../../build.yaml#/components/parameters/id"
  - $ref: "../../../build.yaml#/components/parameters/_format"

  # query parameters
  - $ref: "../../../build.yaml#/components/parameters/isoCode"
